
# Language setting
language: go

# Version number
go:
  #- 1.1
  #- 1.2
  - 1.3

env:
  - CI_REPORT=shippable/testresults/junit.xml

build:
  pre_ci:
    - docker build -t=shippabledocker/pipeline:test25oct .
    
  pre_ci_boot:
    image_name: shippabledocker/pipeline
    image_tag: latest
    pull: false
   
  ci:
    - echo "test"
    #- go version
   # - export GOPATH=$SHIPPABLE_GOPATH
   # - source $HOME/.gvm/scripts/gvm
   # - gvm install go$SHIPPABLE_GO_VERSION
    #- gvm use go$SHIPPABLE_GO_VERSION
    
  post_ci:
    - docker push shippabledocker/pipelinebbtest:test25oct
    
  cache: true
  
  #cache_dir_list:    
   # - $SHIPPABLE_BUILD_DIR/sample_go.go
    
integrations:
  hub:
    - integrationName: plv2-docker
      type: docker
      
